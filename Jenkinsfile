#!/usr/bin/groovy
import groovy.json.JsonOutput

pipeline {
    agent { 
                label 'testa'
            }

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
